/**************************************************/

/* 정수 제곱근 판별 */

/**************************************************/

/* 정답 */
function solution(n) {
    const sqrt = Math.sqrt(n);
    const isSqrt = sqrt === Math.floor(sqrt);

    return isSqrt ? (sqrt + 1) * (sqrt + 1) : -1;
}

/**************************************************/
